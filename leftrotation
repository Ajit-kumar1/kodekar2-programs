#include<stdio.h>
int main()
{
    int n,t,p=0,temp1,j;
    scanf("%d%d",&n,&t);
    int arr[n],temp=0,i;
    for(i=0;i<n;i++)
    scanf("%d",&arr[i]);
        temp=arr[0];
    for(j=t;j>0;j--)
   {
       
       // temp1=arr[n-1];
   for(i=0;i<n-1;i++)
   {
   
        arr[i]=arr[i+1];
        //temp1=arr[0];
    }
        arr[n-1]=temp;
        temp=arr[0];
       // arr[n-2]=temp1;
}
    
    for(i=0;i<n;i++)
        printf("%d ",arr[i]);
}
